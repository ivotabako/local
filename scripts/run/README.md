Run the local stack from the workspace root:

```powershell
./scripts/run/Start-LocalStack.ps1
```

Optional flags:

```powershell
./scripts/run/Start-LocalStack.ps1 -SkipFrontend
./scripts/run/Start-LocalStack.ps1 -SkipBackend
```
